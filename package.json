{
  "name": "organic-ui-root",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "bun run --filter organic-ui  build",
    "dev": "bun --filter landing dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish-lib": "bun --filter organic-ui publish"
  },
  "workspaces": [
    "lib",
    "landing"
  ]
}
